---
title: Security
---

This package provides the API for all Security modules.

<Callout type='info'>
  For an overview of the module, read the [Security guide](../security).
</Callout>

## Initializable

### Initializable

[View on GitHub](https://github.com/OpenZeppelin/compact-contracts/blob/main/contracts/src/security/Initializable.compact)

```ts
import "./node-modules/@openzeppelin-compact/contracts/src/security/Initializable";
```

---

### Ledger [!toc]

#### _isInitialized [toc] [#_isInitialized]
#### [!toc] [#_isInitialized]
<APIItemCompact
  circuitSig='_isInitialized: Boolean'
  kind="ledger"
  id="_isInitialized"
>
  Boolean indicating if initialized.
</APIItemCompact>

### Witnesses [!toc]

None.

### Circuits [!toc]

#### initialize [toc] [#initialize]
#### [!toc] [#initialize]
<APIItemCompact
  circuitSig='initialize() → []'
  kind="circuit"
  complexity="k=10, rows=38"
>
  Initializes the state thus ensuring the calling circuit can only be called once.

  Requirements:

  * Contract must not be initialized.
</APIItemCompact>

#### assertInitialized [toc] [#assertInitialized]
#### [!toc] [#assertInitialized]
<APIItemCompact
  circuitSig='assertInitialized() → []'
  kind="circuit"
  complexity="k=10, rows=31"
>
  Asserts that the contract has been initialized, throwing an error if not.

  Requirements:

  * Contract must not be initialized.
</APIItemCompact>

#### assertNotInitialized [toc] [#assertNotInitialized]
#### [!toc] [#assertNotInitialized]
<APIItemCompact
  circuitSig='assertNotInitialized() → []'
  kind="circuit"
  complexity="k=10, rows=35"
>
  Asserts that the contract has not been initialized, throwing an error if it has.

  Requirements:

  * Contract must not be initialized.
</APIItemCompact>

## Pausable

### Pausable

[View on GitHub](https://github.com/OpenZeppelin/compact-contracts/blob/main/contracts/src/security/Pausable.compact)

```ts
import "./node-modules/@openzeppelin-compact/contracts/src/security/Pausable";
```

### Ledger [!toc]

#### _isPaused [toc] [#_isPaused]
#### [!toc] [#_isPaused]
<APIItemCompact
  circuitSig='_isPaused: Boolean'
  kind="ledger"
  id="_isPaused"
>
  Boolean indicating if paused.
</APIItemCompact>

### Circuits [!toc]

#### isPaused [toc] [#isPaused]
#### [!toc] [#isPaused]
<APIItemCompact
  circuitSig='isPaused() → Boolean'
  kind="circuit"
  complexity="k=10, rows=32"
>
  Returns true if the contract is paused, and false otherwise.
</APIItemCompact>

#### assertPaused [toc] [#assertPaused]
#### [!toc] [#assertPaused]
<APIItemCompact
  circuitSig='assertPaused() → []'
  kind="circuit"
  complexity="k=10, rows=31"
>
  Makes a circuit only callable when the contract is paused.

  Requirements:

  * Contract must be paused.
</APIItemCompact>

#### assertNotPaused [toc] [#assertNotPaused]
#### [!toc] [#assertNotPaused]
<APIItemCompact
  circuitSig='assertNotPaused() → []'
  kind="circuit"
  complexity="k=10, rows=35"
>
  Makes a circuit only callable when the contract is not paused.

  Requirements:

  * Contract must not be paused.
</APIItemCompact>

#### _pause [toc] [#_pause]
#### [!toc] [#_pause]
<APIItemCompact
  circuitSig='_pause() → []'
  kind="circuit"
  complexity="k=10, rows=38"
>
  Triggers a stopped state.

  Requirements:

  * Contract must not be paused.
</APIItemCompact>

#### _unpause [toc] [#_unpause]
#### [!toc] [#_unpause]
<APIItemCompact
  circuitSig='_unpause() → []'
  kind="circuit"
  complexity="k=10, rows=34"
>
  Lifts the pause on the contract.

  Requirements:

  * Contract must be paused.
</APIItemCompact>
