---
title: Utils
---

This package provides the API for all Utils modules.

<Callout>
For an overview of the module, read the [Utils guide](utils).
</Callout>

## Utils

### Utils

[View on GitHub](https://github.com/OpenZeppelin/compact-contracts/blob/main/contracts/src/utils/Utils.compact)

```ts
import "./node-modules/@openzeppelin-compact/contracts/src/security/Initializable";
```

<Callout>
There’s no easy way to get the constraints of pure circuits at this time so the constraints of the circuits listed below have been omitted.
</Callout>

**Circuits:**

- [`isKeyOrAddressZero(keyOrAddress)`](#iskeyoraddresszero)
- [`isKeyZero(key)`](#iskeyzero)
- [`isKeyOrAddressEqual(keyOrAddress, other)`](#iskeyoraddressequal)
- [`isContractAddress(keyOrAddress)`](#iscontractaddress)
- [`emptyString()`](#emptystring)

#### isKeyOrAddressZero

```ts
isKeyOrAddressZero(keyOrAddress: Either<ZswapCoinPublicKey, ContractAddress>) → Boolean
```

Returns whether `keyOrAddress` is the zero address.

<Callout>
Midnight's burn address is represented as `left<ZswapCoinPublicKey, ContractAddress>(default<ZswapCoinPublicKey>)` in Compact, so we've chosen to represent the zero address as this structure as well.
</Callout>

#### isKeyZero

```ts
isKeyZero(key: ZswapCoinPublicKey) → Boolean
```

Returns whether `key` is the zero address.

#### isKeyOrAddressEqual

```ts
isKeyOrAddressEqual(keyOrAddress: Either<ZswapCoinPublicKey, ContractAddress>, other: Either<ZswapCoinPublicKey, ContractAddress>) → Boolean
```

Returns whether `keyOrAddress` is equal to `other`. Assumes that a `ZswapCoinPublicKey` and a `ContractAddress` can never be equal.

#### isContractAddress

```ts
isContractAddress(keyOrAddress: Either<ZswapCoinPublicKey, ContractAddress>) → Boolean
```

Returns whether `keyOrAddress` is a `ContractAddress` type.

#### emptyString

```ts
emptyString() → Opaque<"string">
```

A helper function that returns the empty string: `""`.
