---
title: Utils
---

This package provides the API for all Utils modules.

<Callout type='info'>
  For an overview of the module, read the [Utils guide](/contracts-compact/utils).
</Callout>

## Utils [toc] [#Utils]
<APIGithubLinkHeader
    moduleName="Utils"
    link={`https://github.com/OpenZeppelin/compact-contracts/blob/main/contracts/src/utils/Utils.compact`}
/>

```ts
import "./compact-contracts/node_modules/@openzeppelin-compact/contracts/src/security/Initializable";
```

---

<Callout>
  There’s no easy way to get the constraints of circuits at this time so the constraints of the circuits listed below have been omitted.
</Callout>

### Ledger [toc] [#Utils-Ledger]
### Ledger [!toc] [#Utils-Ledger]

None.

### Witnesses [toc] [#Utils-Witnesses]
### Witnesses [!toc] [#Utils-Witnesses]

None.

### Circuits [toc] [#Utils-Circuits]
### Circuits [!toc] [#Utils-Circuits]

#### isKeyOrAddressZero [toc] [#Utils-isKeyOrAddressZero]
#### [!toc] [#Utils-isKeyOrAddressZero]
<APIItemCompact
  circuitSig='isKeyOrAddressZero(keyOrAddress: Either<ZswapCoinPublicKey, ContractAddress>) → Boolean'
  kind="circuit"
  stackParams="true"
  modulePrefix="Utils"
>
  Returns whether `keyOrAddress` is the zero address.

  <Callout>
    Midnight's burn address is represented as `left<ZswapCoinPublicKey, ContractAddress>(default<ZswapCoinPublicKey>)` in Compact,
    so we've chosen to represent the zero address as this structure as well.
  </Callout>
</APIItemCompact>

#### isKeyZero [toc] [#Utils-isKeyZero]
#### [!toc] [#Utils-isKeyZero]
<APIItemCompact
  circuitSig='isKeyZero(key: ZswapCoinPublicKey) → Boolean'
  kind="circuit"
  modulePrefix="Utils"
>
  Returns whether `key` is the zero address.
</APIItemCompact>

#### isKeyOrAddressEqual [toc] [#Utils-isKeyOrAddressEqual]
#### [!toc] [#Utils-isKeyOrAddressEqual]
<APIItemCompact
  circuitSig='isKeyOrAddressEqual(keyOrAddress: Either<ZswapCoinPublicKey, ContractAddress>, other: Either<ZswapCoinPublicKey, ContractAddress>) → Boolean'
  kind="circuit"
  stackParams="true"
  modulePrefix="Utils"
>
  Returns whether `keyOrAddress` is equal to `other`.
  Assumes that a `ZswapCoinPublicKey` and a `ContractAddress` can never be equal.
</APIItemCompact>

#### isContractAddress [toc] [#Utils-isContractAddress]
#### [!toc] [#Utils-isContractAddress]
<APIItemCompact
  circuitSig='isContractAddress(keyOrAddress: Either<ZswapCoinPublicKey, ContractAddress>) → Boolean'
  kind="circuit"
  stackParams="true"
  modulePrefix="Utils"
>
  Returns whether `keyOrAddress` is a `ContractAddress` type.
</APIItemCompact>

#### emptyString [toc] [#Utils-emptyString]
#### [!toc] [#Utils-emptyString]
<APIItemCompact
  circuitSig='emptyString() → Opaque<"string">'
  kind="circuit"
  modulePrefix="Utils"
>
  A helper function that returns the empty string: `""`.
</APIItemCompact>
