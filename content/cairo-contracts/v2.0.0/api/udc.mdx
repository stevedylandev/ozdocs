---
title: Universal Deployer
description: Universal Deployer
---

Reference of the Universal Deployer Contract (UDC) interface and preset.

## Interfaces

> **Note**
> Starting from version `3.x.x`, the interfaces are no longer part of the `openzeppelin_utils` package. The references documented here are contained in the `openzeppelin_interfaces`.

### `IUniversalDeployer` [🔗](https://github.com/OpenZeppelin/cairo-contracts/blob/release-v3.0.0-alpha.0/packages/interfaces/src/utils/deployments.cairo)

```rust
use openzeppelin_interfaces::deployments::IUniversalDeployer;
```

**Functions**

* [`deploy_contract(class_hash, salt, not_from_zero, calldata)`](#deploy_contract-external)

**Events**

* [`ContractDeployed(address, deployer, not_from_zero, class_hash, calldata, salt)`](#contractdeployed-event)

#### Functions

#### `deploy_contract(class_hash: ClassHash, salt: felt252, not_from_zero: bool, calldata: Span<felt252>) → ContractAddress` *external*

Deploys a contract through the Universal Deployer Contract.

#### Events

#### `ContractDeployed(address: ContractAddress, deployer: ContractAddress, not_from_zero: bool, class_hash: ClassHash, calldata: Span<felt252>, salt: felt252)` *event*

Emitted when `deployer` deploys a contract through the Universal Deployer Contract.

## Presets

### `UniversalDeployer` [🔗](https://github.com/OpenZeppelin/cairo-contracts/blob/release-v3.0.0-alpha.0/packages/presets/src/universal_deployer.cairo)

```rust
use openzeppelin_presets::UniversalDeployer;
```

The standard Universal Deployer Contract.

**Embedded Implementations**

**UniversalDeployerImpl**

* [`deploy_contract(self, address, deployer, not_from_zero, class_hash, calldata, salt)`](#deploy_contract-preset-external)

#### `deploy_contract(ref self: ContractState, address: ContractAddress, deployer: ContractAddress, not_from_zero: bool, class_hash: ClassHash, calldata: Span<felt252>, salt: felt252) -> ContractAddress` *external*

Deploys a contract through the Universal Deployer Contract.

When `not_from_zero` is `true`, `salt` is hashed with the caller address and the modified salt is passed to the inner `deploy_syscall`. This type of deployment is [origin-dependent](/udc.adoc#origin_dependent).

When `not_from_zero` is `false`, the deployment type is [origin-independent](/udc.adoc#origin_independent).

Emits a [ContractDeployed](#contractdeployed-event) event.
